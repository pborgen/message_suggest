{
  "name": "message-suggest",
  "private": true,
  "packageManager": "pnpm@9.15.2",
  "workspaces": [
    "services/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.1.3"
  }
}
